
describe("DCT Decompress in the browser", function() {
	
	it("decompress function should be visible", function() {
		expect(_decompress).to.not.be.undefined;
		expect(typeof _decompress).to.equal('function');
	})
	
})